import { BrowserRouter, Routes, Route, Navigate, useLocation } from "react-router-dom";
import { AuthProvider, useAuth } from "./context/AuthContext";
import { LanguageProvider, useLanguage } from "./context/LanguageContext";
import Login from "./pages/Login"; import Register from "./pages/Register"; import Welcome from "./pages/Welcome"; import StudentDashboard from "./pages/StudentDashboard"; import AdminDashboard from "./pages/AdminDashboard"; import Regulations from "./pages/Regulations"; import Blocked from "./pages/Blocked";
function Gate(){ const {user,profile,loading,isAnyAdmin,blacklisted}=useAuth(); const {t}=useLanguage(); useLocation(); if(loading)return <div className="min-h-screen flex items-center justify-center bg-sand-50 font-body text-ink/50">{t("loading")}</div>; if(!user)return <Login/>; if(blacklisted)return <Blocked/>; if(!profile)return <Register/>; return <Routes><Route path="/about" element={<Welcome/>}/><Route path="/regulations" element={<Regulations/>}/><Route path="/" element={<StudentDashboard/>}/><Route path="/admin" element={isAnyAdmin?<AdminDashboard/>:<Navigate to="/" replace/>}/><Route path="*" element={<Navigate to="/" replace/>}/></Routes>; }
export default function App(){ return <BrowserRouter><LanguageProvider><AuthProvider><Gate/></AuthProvider></LanguageProvider></BrowserRouter>; }
