import { useAuth } from "../context/AuthContext";
import { useLanguage } from "../context/LanguageContext";
export default function Blocked(){ const { signOut }=useAuth(); const {t}=useLanguage(); return <div className="min-h-screen bg-sand-50 flex items-center justify-center p-6"><div className="max-w-md text-center card p-8"><img src="/logo.png" className="w-24 h-24 mx-auto mb-4 object-contain" alt=""/><h1 className="font-display font-extrabold text-2xl text-bad">{t("blockedTitle")}</h1><p className="font-body text-ink/65 mt-3 leading-7">{t("blockedText")}</p><button onClick={signOut} className="mt-6 bg-teal-900 text-white px-6 py-2.5 rounded-xl font-body">{t("logout")}</button></div></div> }
