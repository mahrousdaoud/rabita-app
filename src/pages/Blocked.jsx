import { useAuth } from "../context/AuthContext";
import { useLanguage } from "../context/LanguageContext";
export default function Blocked(){ const {signOut}=useAuth(); const {t}=useLanguage(); return <div className="min-h-screen bg-sand-50 flex items-center justify-center p-6"><div className="max-w-lg w-full card p-8 text-center"><img src="/logo.png" className="w-24 h-24 object-contain mx-auto mb-5"/><h1 className="font-display font-extrabold text-2xl text-teal-950">{t("blockedTitle")}</h1><p className="font-body text-ink/70 leading-7 mt-3">{t("blockedText")}</p><p className="font-body text-sm text-ink/50 mt-2">{t("contactAdmin")}</p><button onClick={signOut} className="mt-6 bg-teal-900 text-white px-5 py-2.5 rounded-xl font-body font-semibold">{t("logout")}</button></div></div>; }
